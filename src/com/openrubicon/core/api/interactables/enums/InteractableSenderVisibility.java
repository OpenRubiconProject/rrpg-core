package com.openrubicon.core.api.interactables.enums;

// Only apllicable to command senders like discord where theres a differentiation between private and public chats
public enum InteractableSenderVisibility {
    PUBLIC,
    PRIVATE,
    NOT_APPLICABLE
}
